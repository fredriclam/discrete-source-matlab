geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 2.8; theta_ign = 0.01; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3; theta_ign = 0.02; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3; theta_ign = 0.03; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3; theta_ign = 0.04; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3; theta_ign = 0.05; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3; theta_ign = 0.075; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3.1; theta_ign = 0.10; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 3.6; theta_ign = 0.15; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 4.3; theta_ign = 0.20; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 5; theta_ign = 0.25; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 5;
nominal_dim = 6.2; theta_ign = 0.30; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 10;
nominal_dim = 8; theta_ign = 0.35; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 40;
nominal_dim = 10; theta_ign = 0.40; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 60;
nominal_dim = 13; theta_ign = 0.45; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'cylinder'; total_instances = 24; time_limit = 80;
nominal_dim = 16; theta_ign = 0.5; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

% $$$$$$$$$$$$

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.7; theta_ign = 0.01; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.7; theta_ign = 0.02; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.7; theta_ign = 0.03; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.7; theta_ign = 0.04; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.7; theta_ign = 0.05; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 10;
nominal_dim = 0.8; theta_ign = 0.075; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 15;
nominal_dim = 1; theta_ign = 0.1; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 20;
nominal_dim = 1.2; theta_ign = 0.15; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 30;
nominal_dim = 1.7; theta_ign = 0.2; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 40;
nominal_dim = 2.2; theta_ign = 0.25; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 50;
nominal_dim = 2.8; theta_ign = 0.3; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 60;
nominal_dim = 3.6; theta_ign = 0.35; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 60;
nominal_dim = 4.2; theta_ign = 0.4; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 80;
nominal_dim = 5.7; theta_ign = 0.45; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);

geom = 'slab'; total_instances = 24; time_limit = 100;
nominal_dim = 7.3; theta_ign = 0.5; tau_c = 0;
% Call core function
batch_string = hpc_writer_core(batch_string, folder_name, platform,...
geom, theta_ign, tau_c, nominal_dim, total_instances, time_limit);
